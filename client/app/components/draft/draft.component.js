import template from './draft.html';
import controller from './draft.controller';
import './draft.scss';

let draftComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default draftComponent;
